import 'package:flutter/material.dart';

class ColorConstants {
  // Legacy — kept for any remaining references
  static const primaryColor = Color(0xFF003479);
  static const secondaryColor = Color(0xFF38B6FF);
  static const whiteColor = Colors.white;
  static const darkGreyColor = Color(0xFF575756);
  static const lightGrey = Color(0xFFb2b2b2);

  // ── Premium Dark Theme ──────────────────────────────
  static const bgPrimary   = Color(0xFF070B14);
  static const bgSecondary = Color(0xFF0D1117);
  static const bgCard      = Color(0xFF161B27);
  static const bgGlass     = Color(0x0DFFFFFF);
  static const borderGlass = Color(0x1AFFFFFF);

  static const accentCyan   = Color(0xFF00D4FF);
  static const accentPurple = Color(0xFF7C3AED);
  static const accentGold   = Color(0xFFF59E0B);
  static const accentGreen  = Color(0xFF10B981);

  static const textPrimary   = Color(0xFFF0F6FF);
  static const textSecondary = Color(0xFF8892B0);
  static const textMuted     = Color(0xFF64748B);
}
